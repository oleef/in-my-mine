// require("dotenv").config();
// const dev = process.env.NODE_ENV !== "production";
// const next = require("next");
// const app = next({ dev });
// app.prepare().then(() => {
//   app.setAssetPrefix(process.env.APP_DOMAIN);
// });

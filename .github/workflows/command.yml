name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: npm install
        run: npm install
      - name: npm build
        run: npm run build
      - name: npm start
        run: npm start
